Setup an application using Docker:
* First clone the repository
* Then in directory which includes docker-compose.yaml, run in terminal 'docker-compose up'
* Connect the application via machine's public URL at web
