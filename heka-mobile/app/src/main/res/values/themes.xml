<resources>
    <!-- Base application theme. -->
    <style name="Theme.Hekamobile" parent="Theme.MaterialComponents.Light.NoActionBar">
        <item name="android:forceDarkAllowed">false</item>
        <item name="android:windowActionBar">false</item>
        <item name="android:windowNoTitle">true</item>
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/v_red</item>
        <item name="colorPrimaryVariant">@color/v_pink</item>
        <item name="colorOnPrimary">@color/v_white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/v_blue</item>
        <item name="colorSecondaryVariant">@color/v_bblue</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" >?attr/colorOnPrimary</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="Theme.Hekamobile.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.Hekamobile.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.Hekamobile.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
</resources>